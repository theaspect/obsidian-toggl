---
phase: quick-260418-6dn
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/settings.ts
  - styles.css
autonomous: true
requirements: [QUICK-260418-6dn]

must_haves:
  truths:
    - "A 'Connection' section heading appears before the API token row"
    - "A 'Formatting' section heading appears before the Output format row"
    - "Test button appears inline with the API token input in the same Setting row"
    - "Test button is disabled when token field is empty"
    - "Disabled setting rows are visually greyed out"
  artifacts:
    - path: "src/settings.ts"
      provides: "Restructured settings UI with headings, merged API token+test row, row-level disabled state"
    - path: "styles.css"
      provides: "CSS rule making disabled setting rows semi-transparent"
  key_links:
    - from: "API token text onChange"
      to: "testBtn.setDisabled"
      via: "closure reference"
    - from: "styles.css"
      to: ".setting-item.is-disabled"
      via: "Obsidian adds is-disabled class when Setting.setDisabled(true) is called"
---

<objective>
Restructure the settings UI: add section headings, merge test-connection button into the API token row, and grey out disabled setting rows.

Purpose: Makes the settings panel scannable (clear sections), reduces visual noise (test button contextual to its field), and communicates disabled state clearly.
Output: Updated src/settings.ts and styles.css.
</objective>

<execution_context>
@C:/Work/obsidian-toggl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Work/obsidian-toggl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Work/obsidian-toggl/.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add section headings and merge test button into token row</name>
  <files>src/settings.ts</files>
  <action>
Restructure the `display()` method in `TogglImportSettingTab` as follows:

1. **Add a "Connection" heading** — insert `new Setting(containerEl).setName('Connection').setHeading();` before the API token setting.

2. **Merge test button into the API token row** — instead of two separate `Setting` rows (token + test connection), use ONE `Setting` row that has both `.addText(...)` and `.addButton(...)`. Remove the standalone "Test connection" `Setting` entirely.
   - The merged row keeps the name "Toggl API token" and the existing description.
   - The button reference (`testBtn`) still works the same way — captured via `.addButton(btn => { testBtn = btn; ... })` on the merged setting.
   - The `onChange` handler on the text input still calls `testBtn.setDisabled(value.trim() === '')`.
   - The button's click handler logic remains identical to the current implementation.

3. **Add a "Formatting" heading** — insert `new Setting(containerEl).setName('Formatting').setHeading();` before the "Output format" setting (i.e., between "Day wrap time" and "Output format").

4. **Row-level disabled state for Delimiter, Template, and column toggles** — use `Setting.setDisabled()` on the whole setting row instead of (or in addition to) disabling individual controls:
   - Delimiter row: call `.setDisabled(this.plugin.settings.outputFormat !== 'plaintext')` on the Setting object.
   - Template row: call `.setDisabled(this.plugin.settings.outputFormat !== 'template')` on the Setting object.
   - Each column toggle row: call `.setDisabled(isTemplate)` on the Setting object.
   - Keep the existing `.setDisabled(...)` on individual controls too so they remain non-interactive.
   - Obsidian's `Setting.setDisabled(true)` adds the `is-disabled` CSS class to `.setting-item`; the CSS in Task 2 styles that class.

The final order of settings rows:
- [heading] Connection
- Toggl API token (with inline Test button)
- Sort order
- Day wrap time
- [heading] Formatting
- Output format
- Delimiter (disabled when format !== 'plaintext')
- Template (disabled when format !== 'template')
- [heading] Columns
- Description / Start time / Duration / Tags / Project (each disabled when format === 'template')
  </action>
  <verify>
    <automated>cd C:/Work/obsidian-toggl && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds with no TypeScript errors. Settings file contains one merged token+button row, two new headings ("Connection" and "Formatting"), and Setting.setDisabled() calls on Delimiter, Template, and column rows.</done>
</task>

<task type="auto">
  <name>Task 2: Grey out disabled setting rows via CSS</name>
  <files>styles.css</files>
  <action>
Add CSS to `styles.css` that reduces opacity on disabled setting rows. Obsidian applies the `is-disabled` class to `.setting-item` when `Setting.setDisabled(true)` is called.

Add this rule:

```css
.setting-item.is-disabled {
    opacity: 0.45;
}
```

This is sufficient — Obsidian's own disabled controls already prevent interaction; the opacity makes the greyed-out state visually obvious without needing additional pointer-events rules.
  </action>
  <verify>
    <automated>cd C:/Work/obsidian-toggl && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>styles.css contains the .setting-item.is-disabled rule. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    - "Connection" heading above API token row
    - "Formatting" heading above Output format row
    - Test button appears inline to the right of the API token password input in a single row
    - Test button disabled when token field is empty
    - Delimiter, Template, and column toggle rows visually greyed out when disabled
  </what-built>
  <how-to-verify>
    1. Run `npm run build` and copy `main.js` + `styles.css` to your vault's plugin folder.
    2. Open Obsidian, go to Settings -> Toggl Import.
    3. Confirm "Connection" heading appears above the API token field.
    4. Confirm the API token row has a "Test" button inline to the right of the password input.
    5. Clear the token field — confirm the Test button becomes disabled/greyed.
    6. Confirm "Formatting" heading appears above the Output format dropdown.
    7. Set Output format to "Markdown table" — confirm Delimiter and Template rows are visually greyed out.
    8. Set Output format to "Custom template" — confirm Delimiter greyed, Template active, Columns section greyed.
    9. Set Output format to "Plain text" — confirm only Template is greyed.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings UI → stored token | Token entered in password field; stored via loadLocalStorage/saveLocalStorage (unchanged) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6dn-01 | Information Disclosure | API token password field | accept | Field type remains 'password'; no change to storage mechanism |
</threat_model>

<verification>
- `npm run build` exits 0 with no TypeScript errors
- Two new headings visible in settings: "Connection" and "Formatting"
- Single merged row for API token + test button
- Disabled rows render at reduced opacity
</verification>

<success_criteria>
Settings panel has clear "Connection" and "Formatting" section headings. Test button is inline with the API token field and disabled when the field is empty. Disabled setting rows (Delimiter, Template, column toggles) are visually greyed out at 45% opacity.
</success_criteria>

<output>
After completion, create `.planning/quick/260418-6dn-group-formatting-configuration-and-add-a/260418-6dn-SUMMARY.md`
</output>
