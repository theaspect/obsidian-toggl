---
phase: quick-260418-763
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/settings.ts
  - src/main.ts
  - styles.css
autonomous: true
requirements:
  - 260418-763
must_haves:
  truths:
    - "No inline style assignments remain in plugin TypeScript source"
    - "Command name uses sentence case"
    - "styles.css uses only CSS classes (no hardcoded color values or fixed pixel dims not backed by a class)"
  artifacts:
    - path: "styles.css"
      provides: "CSS class for narrow right-aligned day wrap input"
      contains: ".toggl-import-day-wrap-input"
    - path: "src/settings.ts"
      provides: "Settings UI with no inline style assignments"
    - path: "src/main.ts"
      provides: "Command registered with sentence-case name"
  key_links:
    - from: "src/settings.ts"
      to: "styles.css"
      via: "inputEl.addClass('toggl-import-day-wrap-input')"
      pattern: "addClass.*toggl-import"
---

<objective>
Review the plugin against Obsidian's official plugin guidelines and fix all violations found.

Purpose: Ensure the plugin meets Obsidian community plugin submission standards before registry submission.
Output: All guideline violations corrected — inline styles moved to CSS classes, command name in sentence case.
</objective>

<execution_context>
@C:/Work/obsidian-toggl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Work/obsidian-toggl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Work/obsidian-toggl/.planning/PROJECT.md
@C:/Work/obsidian-toggl/.planning/ROADMAP.md
@C:/Work/obsidian-toggl/.planning/STATE.md
</context>

<guidelines_audit>
## Obsidian Plugin Guidelines Audit

Source: https://docs.obsidian.md/Plugins/Releasing/Plugin+guidelines
Fetched: 2026-04-18

### PASS — No violations

| Guideline | Status | Evidence |
|-----------|--------|---------|
| Use `this.app` not global `app` | PASS | All code uses `this.app`, `plugin.app`, or passed `app` reference |
| Minimize console logging | PASS | No `console.*` calls in any source file |
| Rename placeholder class names | PASS | `TogglImportPlugin`, `TogglImportSettingTab` — not sample plugin names |
| No `innerHTML` / `outerHTML` / `insertAdjacentHTML` | PASS | Grep confirms zero uses |
| Use `setHeading()` not HTML heading elements | PASS | `setName(...).setHeading()` used for all section headings |
| Section headings don't include the word "settings" | PASS | Headings: "Connection", "Formatting", "Columns" |
| Use `const`/`let` not `var` | PASS | No `var` in source |
| Use `async`/`await` not Promise chains | PASS | All async code uses await |
| Use Editor API for active file edits | PASS | `editor.replaceSelection()` used |
| Don't set default hotkeys | PASS | No `hotkeys` field in `addCommand()` |
| Use appropriate callback type | PASS | `editorCallback` used — correct for editor-dependent command |
| No hardcoded colors in CSS | PASS | `styles.css` only sets `opacity` — no color values |

### FAIL — Violations to fix

| ID | Guideline | Location | Violation | Fix |
|----|-----------|----------|-----------|-----|
| V-01 | Never hardcode inline styles; use CSS classes | `src/settings.ts:76` | `t.inputEl.style.width = '80px'; t.inputEl.style.textAlign = 'right';` | Move to `.toggl-import-day-wrap-input` CSS class, apply via `inputEl.addClass()` |
| V-02 | Apply sentence case to all UI elements | `src/main.ts:50` | `name: 'Import Toggl Entries'` — "Entries" should be lowercase | Change to `'Import Toggl entries'` |
</guidelines_audit>

<tasks>

<task type="auto">
  <name>Task 1: Fix V-01 — move day wrap input inline styles to CSS class</name>
  <files>src/settings.ts, styles.css</files>
  <action>
In `styles.css`, add a new rule after the existing `.setting-item.is-disabled` block:

```css
.toggl-import-day-wrap-input {
    width: 80px;
    text-align: right;
}
```

In `src/settings.ts`, replace the `.then()` callback on the day wrap time text input (currently line 76):

BEFORE:
```typescript
.then(t => { t.inputEl.style.width = '80px'; t.inputEl.style.textAlign = 'right'; })
```

AFTER:
```typescript
.then(t => { t.inputEl.addClass('toggl-import-day-wrap-input'); })
```

The `addClass()` method is available on all Obsidian `HTMLElement` subtypes — no import needed. This eliminates the inline style violation (V-01) while keeping identical visual behaviour.
  </action>
  <verify>
    <automated>grep -n "style\." src/settings.ts; grep -n "toggl-import-day-wrap-input" src/settings.ts styles.css</automated>
  </verify>
  <done>
    - `grep "style\."` on `src/settings.ts` returns no matches (zero inline style assignments)
    - `grep "toggl-import-day-wrap-input"` matches one line in `src/settings.ts` (the `addClass` call) and one rule block in `styles.css`
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix V-02 — sentence case command name</name>
  <files>src/main.ts</files>
  <action>
In `src/main.ts`, change the command `name` field from:

```typescript
name: 'Import Toggl Entries',
```

to:

```typescript
name: 'Import Toggl entries',
```

"Toggl" stays capitalised (proper noun / brand name). "entries" is lowercased per Obsidian sentence-case rule.
  </action>
  <verify>
    <automated>grep -n "name:" src/main.ts</automated>
  </verify>
  <done>
    - `grep` shows `name: 'Import Toggl entries'` with lowercase "entries"
    - No other UI text sentence-case issues present
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS class injection | Plugin-owned CSS class applied to Obsidian DOM — no user input involved, no threat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-763-01 | Tampering | CSS class name | accept | Class name is plugin-defined constant, not user-supplied; no injection vector |
</threat_model>

<verification>
After both tasks complete:

1. `grep -rn "style\." src/` returns no matches — zero inline style assignments remain
2. `grep -n "toggl-import-day-wrap-input" src/settings.ts styles.css` shows the class used in settings and defined in CSS
3. `grep "Import Toggl" src/main.ts` shows `Import Toggl entries` (lowercase e)
4. `npm run build` completes without TypeScript errors
</verification>

<success_criteria>
- All 12 guideline checks remain PASS
- V-01 resolved: day wrap input styled via CSS class, no inline style properties
- V-02 resolved: command name reads "Import Toggl entries"
- Plugin builds cleanly: `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/quick/260418-763-review-plugin-guidelines-and-verify-adheren/260418-763-SUMMARY.md`
</output>
