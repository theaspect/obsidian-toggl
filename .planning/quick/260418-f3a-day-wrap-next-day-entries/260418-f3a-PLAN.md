---
quick_id: 260418-f3a
slug: day-wrap-next-day-entries
description: "day wrap: include next-day entries before wrap time"
date: 2026-04-18
status: in-progress
must_haves:
  truths:
    - When dayWrapTime is non-zero, fetchTimeEntries extends end_date to next_day@wrapTime
    - Entries from next day whose local start < wrapTime are included in results
    - Entries from current day whose local start < wrapTime are still excluded (existing behavior preserved)
    - All existing tests continue to pass
  artifacts:
    - src/api.ts updated
    - tests/api.test.ts updated with 3 new tests
---

# Quick Task 260418-f3a: day wrap — include next-day entries before wrap time

## Tasks

### Task 1: Extend date range and filter in fetchTimeEntries (src/api.ts)

**Files:** `src/api.ts`

**Action:**
1. Move wrap time parsing above the `end` date construction
2. Add `addOneDay(date)` helper function
3. When `wrapMinutes > 0`, set `end = new Date(nextDay + 'T' + HH + ':' + MM + ':00').toISOString()`
4. Update the wrap filter to distinguish current-day vs next-day entries:
   - Current day: keep entries where `startLocalMinutes >= wrapMinutes` (unchanged)
   - Next day: keep entries where `startLocalMinutes < wrapMinutes` (new)

**Verify:** `npm run build` (tsc + esbuild)

### Task 2: Add tests for next-day inclusion (tests/api.test.ts)

**Files:** `tests/api.test.ts`

**Action:** Add 3 tests to the `day wrap time filter` describe block:
1. end_date extends past current day when wrap is set
2. next-day entries before wrap time are included
3. next-day entries at/after wrap time are excluded

**Verify:** `npm test` — all 79 + 3 new = 82 tests pass
