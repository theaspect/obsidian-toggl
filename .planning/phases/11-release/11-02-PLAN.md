---
phase: 11-release
plan: 02
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified: []
autonomous: false
requirements:
  - REL-02

must_haves:
  truths:
    - "All registry checklist items pass before PR is opened"
    - "A submission PR exists on obsidianmd/obsidian-releases adding this plugin"
  artifacts:
    - path: "manifest.json"
      provides: "Valid manifest with all required fields"
      contains: "obsidian-toggl-import"
    - path: "LICENSE"
      provides: "License file required by registry"
      contains: "MIT License"
    - path: "README.md"
      provides: "Documentation required by registry"
      contains: "# Toggl Import"
  key_links:
    - from: "community-plugins.json (obsidianmd/obsidian-releases)"
      to: "manifest.json"
      via: "plugin id and repo reference"
      pattern: "obsidian-toggl-import"
---

<objective>
Verify all Obsidian community plugin registry requirements are met and open a submission PR to obsidianmd/obsidian-releases.

Purpose: REL-02 requires the plugin to meet all registry requirements and have a submission PR opened.
Output: A pull request on obsidianmd/obsidian-releases adding obsidian-toggl-import to community-plugins.json.
</objective>

<execution_context>
@C:/Work/obsidian-toggl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Work/obsidian-toggl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-release/11-CONTEXT.md
@.planning/phases/11-release/11-01-SUMMARY.md

@manifest.json
@package.json
@versions.json
@LICENSE
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-submission checklist verification</name>
  <files></files>
  <read_first>manifest.json, package.json, versions.json, LICENSE, README.md</read_first>
  <action>
Run a comprehensive pre-submission checklist (per D-10) verifying ALL Obsidian community plugin registry requirements:

1. **manifest.json validation:**
   - `id` field exists and is kebab-case: `obsidian-toggl-import`
   - `name` field exists: `Toggl Import`
   - `author` field is non-empty: `Constantine`
   - `authorUrl` field is valid URL: `https://github.com/theaspect`
   - `description` field is non-empty
   - `version` is valid semver: `1.1.0`
   - `minAppVersion` is valid semver: `1.8.7`
   - `isDesktopOnly` is boolean

2. **Required files exist:**
   - `LICENSE` file present at repo root
   - `README.md` file present at repo root
   - `manifest.json` present at repo root
   - `main.js` can be built (run `npm run build`)

3. **No hardcoded credentials:**
   - Grep src/ for patterns like hardcoded API tokens, passwords, secrets
   - Confirm API token is loaded from localStorage, not embedded in source

4. **No external asset loading from untrusted domains:**
   - Grep src/ for `fetch(` calls — should only hit `api.toggl.com`
   - No CDN imports, no external script loading

5. **CI passing:**
   - Run `npm test` to verify tests pass
   - Run `npm run build` to verify clean build

6. **Screenshot is real (not placeholder):**
   - Check `assets/demo.png` file size is > 1KB (placeholder would be tiny)

Print a checklist summary with PASS/FAIL for each item. If any FAIL, stop and report.
  </action>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npm test` exits 0
    - No grep hits for hardcoded tokens in src/ (no literal base64 auth strings)
    - All fetch calls in src/ target only `api.toggl.com`
    - assets/demo.png file size is greater than 1024 bytes
  </acceptance_criteria>
  <done>All pre-submission checklist items pass; build and tests green</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Confirm readiness to open submission PR to obsidianmd/obsidian-releases</decision>
  <context>
Opening a PR to the Obsidian community plugin registry is a public action. Once opened, the Obsidian team will review the plugin. This checkpoint confirms:
1. All checklist items from Task 1 passed
2. The screenshot in assets/demo.png is a real screenshot (not placeholder)
3. The README content is finalized
4. You are ready for the plugin to be publicly listed

The PR will add one entry to community-plugins.json:
```json
{
    "id": "obsidian-toggl-import",
    "name": "Toggl Import",
    "author": "Constantine",
    "description": "Import Toggl Track time entries into daily notes with a single command.",
    "repo": "theaspect/obsidian-toggl-import"
}
```
  </context>
  <options>
    <option id="proceed">
      <name>Open the submission PR now</name>
      <pros>Plugin gets into review queue; everything is ready</pros>
      <cons>None if checklist passed</cons>
    </option>
    <option id="defer">
      <name>Defer submission — not ready yet</name>
      <pros>More time to polish</pros>
      <cons>Delays registry listing</cons>
    </option>
  </options>
  <resume-signal>Select: proceed or defer</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Open community plugin registry submission PR</name>
  <files></files>
  <read_first>manifest.json</read_first>
  <action>
Open a PR to `obsidianmd/obsidian-releases` on GitHub (per D-09).

Steps:
1. Fork `obsidianmd/obsidian-releases` if not already forked (use `gh repo fork obsidianmd/obsidian-releases --clone=false`)
2. Clone the fork: `gh repo clone theaspect/obsidian-releases -- --depth=1`
3. Create a branch: `git checkout -b add-obsidian-toggl-import`
4. Edit `community-plugins.json`: add a new entry in the JSON array (maintain alphabetical order by id). The entry to add:
   ```json
   {
       "id": "obsidian-toggl-import",
       "name": "Toggl Import",
       "author": "Constantine",
       "description": "Import Toggl Track time entries into daily notes with a single command.",
       "repo": "theaspect/obsidian-toggl-import"
   }
   ```
5. Commit with message: `Add obsidian-toggl-import plugin`
6. Push the branch: `git push -u origin add-obsidian-toggl-import`
7. Open PR using `gh pr create` targeting `obsidianmd/obsidian-releases` with:
   - Title: `Add plugin: Toggl Import`
   - Body: Follow the obsidian-releases PR template. Include:
     - Repo URL: https://github.com/theaspect/obsidian-toggl-import
     - Brief description of what the plugin does
     - Confirmation that all checklist items are met (LICENSE, README, valid manifest, no hardcoded secrets, desktop only)

After opening, record the PR URL in the summary.
  </action>
  <verify>
    <automated>echo "PR opened — verify URL returned by gh pr create"</automated>
  </verify>
  <acceptance_criteria>
    - A PR exists on obsidianmd/obsidian-releases (gh pr view returns data)
    - The PR title contains "Toggl Import"
    - The PR adds an entry with id "obsidian-toggl-import" to community-plugins.json
    - The PR targets the obsidianmd/obsidian-releases default branch
  </acceptance_criteria>
  <done>Submission PR opened on obsidianmd/obsidian-releases; PR URL recorded</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub API (gh CLI) | Authenticated via user's gh session; PR opened under user's GitHub account |
| obsidianmd/obsidian-releases | Public repo; PR is a public action reviewed by Obsidian team |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | I (Information Disclosure) | Submission PR | accept | PR content is intentionally public (plugin listing). No secrets in PR body. |
| T-11-04 | S (Spoofing) | gh CLI auth | accept | Uses user's existing gh authentication; no new credentials created. Checkpoint decision gate ensures user explicitly approves the public action. |
</threat_model>

<verification>
- Pre-submission checklist passes (build, tests, no secrets, LICENSE, README)
- PR exists on obsidianmd/obsidian-releases with correct plugin entry
- PR URL accessible and shows correct content
</verification>

<success_criteria>
- All registry requirements verified before submission
- Submission PR opened on obsidianmd/obsidian-releases
- PR adds obsidian-toggl-import entry to community-plugins.json
- PR URL documented in summary
</success_criteria>

<output>
After completion, create `.planning/phases/11-release/11-02-SUMMARY.md`
</output>
