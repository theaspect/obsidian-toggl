---
phase: 07-ci-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: true
requirements:
  - CI-02
must_haves:
  truths:
    - "npm install completes with zero peer dependency warnings related to obsidian, @codemirror/state, or @codemirror/view"
    - "@codemirror/state is pinned to exactly 6.5.0 (not a caret range) in package.json devDependencies"
    - "@codemirror/view is pinned to exactly 6.38.6 (not a caret range) in package.json devDependencies"
    - "npm run build still succeeds after the version change"
  artifacts:
    - path: "package.json"
      provides: "Pinned @codemirror/state and @codemirror/view devDependency versions matching obsidian 1.12.3 peer requirements"
      contains: '"@codemirror/state": "6.5.0"'
    - path: "package-lock.json"
      provides: "Regenerated lockfile reflecting the pinned @codemirror versions"
      contains: "6.5.0"
  key_links:
    - from: "package.json devDependencies"
      to: "obsidian@1.12.3 peerDependencies"
      via: "exact version pin"
      pattern: '"@codemirror/state":\s*"6\.5\.0"'
---

<objective>
Resolve the npm peer dependency conflict between `obsidian@1.12.3` and `@codemirror/state` / `@codemirror/view` so that `npm install` runs cleanly in CI without warnings or `ERESOLVE` errors.

Purpose: Satisfies CI-02. Unblocks the GitHub Actions CI pipeline (Plan 02) and any future install in a cold environment.

Output: Updated `package.json` with exact-pinned codemirror devDependencies and a regenerated `package-lock.json`.
</objective>

<execution_context>
@C:/Work/obsidian-toggl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Work/obsidian-toggl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-ci-fixes/07-CONTEXT.md
@package.json

<interfaces>
<!-- Current devDependencies state (extracted from package.json at planning time) -->

From package.json:
```json
"devDependencies": {
  "@codemirror/state": "^6.6.0",
  "@codemirror/view": "^6.41.0",
  "@types/node": "^16.11.6",
  "esbuild": "0.28.0",
  "obsidian": "latest",
  "tslib": "^2.8.1",
  "typescript": "6.0.2",
  "vitest": "^4.1.4"
}
```

Target state per D-01 in 07-CONTEXT.md:
- `@codemirror/state` must become `"6.5.0"` (no caret — exact pin)
- `@codemirror/view` must become `"6.38.6"` (no caret — exact pin)
- All other devDependencies unchanged
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin @codemirror/state and @codemirror/view to obsidian 1.12.3 peer versions</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current devDependencies must be preserved except the two codemirror entries)
    - .planning/phases/07-ci-fixes/07-CONTEXT.md (D-01 specifies the exact target versions)
  </read_first>
  <action>
  Edit `package.json` devDependencies to change ONLY these two entries:

  - `"@codemirror/state": "^6.6.0"` → `"@codemirror/state": "6.5.0"` (remove the caret, exact pin)
  - `"@codemirror/view": "^6.41.0"` → `"@codemirror/view": "6.38.6"` (remove the caret, exact pin)

  Do NOT touch any other devDependency. Do NOT add new dependencies. Do NOT change `"obsidian": "latest"` (out of scope for this plan).

  After editing, run `npm install` to regenerate `package-lock.json` with the new versions. The install MUST complete without ERESOLVE errors and without peer dependency warnings mentioning `@codemirror/state` or `@codemirror/view`. If warnings appear, STOP and report — do not mask with `--legacy-peer-deps` or `--force`.

  Then run `npm run build` to confirm the existing build still succeeds (tsc --noEmit + esbuild production). Then run `npm test` to confirm vitest still passes.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.devDependencies['@codemirror/state']!=='6.5.0')process.exit(1); if(p.devDependencies['@codemirror/view']!=='6.38.6')process.exit(1); console.log('OK')"</automated>
    <automated>npm install 2>&1 | tee /tmp/npm-install.log; ! grep -i 'ERESOLVE\|peer dep' /tmp/npm-install.log</automated>
    <automated>npm run build</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains the exact string `"@codemirror/state": "6.5.0"` (no caret, no tilde)
    - `package.json` contains the exact string `"@codemirror/view": "6.38.6"` (no caret, no tilde)
    - `package.json` still contains all other original devDependencies unchanged: `@types/node`, `esbuild`, `obsidian`, `tslib`, `typescript`, `vitest`
    - `package-lock.json` exists and contains version strings `6.5.0` and `6.38.6` for the respective codemirror packages
    - `npm install` output does NOT contain the substring `ERESOLVE`
    - `npm install` output does NOT contain a line matching `peer.*obsidian` as a warning
    - `npm run build` exits with code 0
    - `npm test` exits with code 0
    - No `--legacy-peer-deps` or `--force` flag was used anywhere
  </acceptance_criteria>
  <done>
    package.json pins @codemirror/state to 6.5.0 and @codemirror/view to 6.38.6 exactly; npm install, npm run build, and npm test all succeed without peer-dep warnings; package-lock.json is regenerated and committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → npm registry | devDependency versions fetched from registry.npmjs.org during install |
| CI runner → npm registry | Same install path runs in GitHub Actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | @codemirror/state, @codemirror/view (devDeps) | mitigate | Use exact version pins (no caret) so `package-lock.json` locks a single integrity hash; lockfile committed. Future upgrades are explicit PRs reviewed by the user. |
| T-07-02 | Tampering | package-lock.json integrity hashes | mitigate | Run `npm install` (not `npm install --no-save`) so lockfile is regenerated with SRI hashes for the pinned versions. Commit the lockfile in the same commit as package.json. |
| T-07-03 | Elevation of Privilege | devDependency supply chain | accept | @codemirror packages are published by the well-known Marijnh/codemirror org, already trusted transitively via obsidian peer deps. No new supply-chain surface introduced by this change. Risk unchanged. |
| T-07-04 | Denial of Service | `npm install` hang from peer conflict | mitigate | This plan itself mitigates DoS-of-CI by resolving the ERESOLVE that causes `npm install` to fail in strict mode. Verified by CI run in Plan 02. |
</threat_model>

<verification>
After this plan completes:
1. `grep -n '"@codemirror' package.json` shows exactly two lines, both with pinned (non-caret) versions
2. `git diff package.json` shows only the two intended version changes
3. `npm install` produces no peer-dep warnings
4. `npm run build` succeeds
5. `npm test` succeeds
6. `package-lock.json` is updated and staged for commit
</verification>

<success_criteria>
- CI-02 satisfied: `npm install` resolves cleanly with zero peer dependency warnings or conflicts between obsidian and @codemirror/state / @codemirror/view
- No regression: build and tests still pass locally
- No workaround flags used (--legacy-peer-deps, --force)
</success_criteria>

<output>
After completion, create `.planning/phases/07-ci-fixes/07-01-SUMMARY.md`.
</output>
